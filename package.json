{
  "name": "firewall-api-vercel",
  "version": "0.1.0",
  "description": "Firewall Cafe API - Vercel Serverless Functions",
  "private": true,
  "scripts": {
    "dev": "node local-server.js",
    "vercel-dev": "vercel dev --listen 3001",
    "build": "echo 'No build step required'",
    "deploy": "vercel --prod",
    "test": "jest",
    "start": "node test-db.js"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.901.0",
    "@vercel/postgres": "^0.5.1",
    "axios": "^1.6.2",
    "dotenv": "^16.6.1",
    "dotenv-flow": "^4.1.0",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "jest": "^29.7.0",
    "vercel": "^32.0.0"
  }
}